import express from "express";

const router = express.Router();


// GET all
router.get("/", )

//GET one
router.get("/:id", )

// CREATE task
router.post("/", )

// UPDATE task
router.put("/:id", )

//DELETE task
router.delete("/:id", )

export default router;